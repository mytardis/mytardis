<?xml version="1.0" encoding="utf-8"?>
<registryObjects xmlns="http://ands.org.au/standards/rif-cs/registryObjects"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://ands.org.au/standards/rif-cs/registryObjects http://services.ands.org.au/documentation/rifcs/1.2.0/schema/registryObjects.xsd">
    <registryObject
        group="Australian Synchrotron">
        <key>{{key}}</key>
        <originatingSource>{{originating_source}}</originatingSource>

        <collection type="dataset">
            <identifier type="local">{{experiment.id}}</identifier>
            <name type="primary">
                <namePart>{{experiment.title}}</namePart>
            </name>
            <location>
                <address>
{% if url %}                    
                    <electronic type="url">
                        <value>{{url}}</value>
                    </electronic>
{% endif %}
{% if email %}
                    <electronic type="email">
                        <value>{{email}}</value>
                    </electronic>
{% endif %}
{% if address %}
                    <physical type="streetAddress">
                        <addressPart type="text">
                            {{address}}
                        </addressPart>
                    </physical> 
{% endif %}
                </address>
            </location>
            <coverage>
                <temporal>
                    <date type="dateFrom" dateFormat="UTC">{{experiment.end_time|date:"c"}}</date>
                </temporal>
            </coverage>
{% if produced_by %}
            <relatedObject>
                <key>{{produced_by}}</key>
                <relation type="isProducedBy"/>
            </relatedObject>
{% endif %}

{% if located_in %}
            <relatedObject>
                <key>synchrotron.org.au/mytardis</key>
                <relation type="isLocatedIn"/>
            </relatedObject>
{% endif %}

{% for forcode in anzsrcfor %}
            <subject type="anzsrc-for">{{forcode}}</subject>
{% endfor %}
{% for lcode in localcodes %}
            <subject type="local">{{lcode}}</subject>
{% endfor %}

{% if license_title %}
            <description type="rights">{{license_title}} (link below)</description>
{% endif %}
      <description type="accessRights">This collection of data is released as-is. Where data is publicly accessible, data will
no longer be subject to embargo and can be used freely provided attribution for the source is given. It is encouraged that persons interested in using the data contact the parties listed for this record to obtain guidance and context in applying this data set and to stay informed about upcoming revisions and related releases of interest.
      </description>

            <description type="note">
{% if notes %}
{{notes}}
{% else %}
Information on the format and analysis of the data in this collection is available from the {{beamline_name}} beamline link on this page (Is produced by:).
{% endif %}            
            </description>
            
            <description type="brief">
{{description}}
{% if sample_description_list %}
Samples: 
  
{{sample_description_list}}
{% endif %}
    Analysis of the sample(s) listed above by the {{beamline}} beamline at {{experiment.institution_name}}   
{% if investigator_list %}
Investigators:    
{{investigator_list}}
{% endif %}
            </description>

{% if license_uri %}         
            <relatedInfo>
                <identifier type="uri">{{license_uri}}</identifier>
                <title>{{license_title}}</title>
            </relatedInfo>            
{% endif %}

{% for rel_inf in uri_related_info_list %}
            <relatedInfo>
                <identifier type="uri">{{rel_inf.uri}}</identifier>
                <title>{{rel_inf.title}}</title>
                <notes>{{rel_inf.notes}}</notes>
            </relatedInfo>         
{% endfor %}
{% for rel_inf in publication_related_info_list %}
            <relatedInfo>
                <identifier type="publication">{{rel_inf.uri}}</identifier>
                <title>{{rel_inf.title}}</title>
                <notes>{{rel_inf.notes}}</notes>
            </relatedInfo>         
{% endfor %}
        </collection>

    </registryObject>
</registryObjects>